# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AccountManager' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AccountManager

  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'

  target 'AccountManagerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AccountManagerUITests' do
    # Pods for testing
  end

end
